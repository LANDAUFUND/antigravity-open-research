# ПРОТОКОЛ НЕЗАВИСИМОЙ РЕПЛИКАЦИИ АНТИГРАВИТАЦИИ

АНТИГРАВИТАЦИЯ РАССМАТРИВАЕТСЯ КАК ИССЛЕДОВАТЕЛЬСКОЕ НАПРАВЛЕНИЕ, ТРЕБУЮЩЕЕ МАТЕМАТИЧЕСКОЙ ФОРМАЛИЗАЦИИ, ЭКСПЕРИМЕНТАЛЬНОЙ ПРОВЕРКИ, ИЗМЕРИМОСТИ И ВОСПРОИЗВОДИМОСТИ.

## 1. НАЗНАЧЕНИЕ

Документ задает порядок независимого повторения лабораторного результата. Репликация нужна для отделения устойчивого физического эффекта от ошибки стенда, локального артефакта, настройки оператора, обработки данных или неполного контроля известной физики.

## 2. ОБЛАСТЬ ПРИМЕНЕНИЯ

Протокол применяется к любой серии, где заявлен остаточный сигнал после учета механики, электромагнетизма, электростатики, тепла, воздуха, плазмы, химии, вибраций и ошибок датчика.

## 3. УРОВНИ РЕПЛИКАЦИИ

| Уровень | Кто выполняет | Что считается результатом |
| --- | --- | --- |
| `R0` | исходная команда | первичная серия с полным raw data |
| `R1` | другая смена на том же стенде | проверка операторского фактора |
| `R2` | другой стенд той же лаборатории | проверка зависимости от оборудования |
| `R3` | независимая лаборатория | проверка внешней воспроизводимости |
| `R4` | открытый публичный протокол | проверка сообществом без закрытых параметров |

Статус `R3` или выше не означает, что антигравитация доказана. Он означает только, что наблюдаемый эффект пережил более строгий уровень повторения.

## 4. МИНИМАЛЬНЫЕ УСЛОВИЯ ДОПУСКА

- зафиксирован исходный протокол опыта;
- есть raw data без ручного удаления неудобных точек;
- есть протокол калибровки датчиков;
- есть контрольные серии без активного режима;
- есть карта электромагнитных и тепловых условий;
- заранее записан критерий успеха и критерий провала;
- указаны версии оборудования и обработки данных;
- отрицательные серии не скрыты.

## 5. ОСНОВНЫЕ РАСЧЕТНЫЕ ВЕЛИЧИНЫ

```text
P = m · g
ΔP = Pконтроль - Pактивный
Fостаток = ΔP - Fмех - Fэм - Fэлст - Fаэро - Fтепл - Fплазма - Fдатчик
SNR = |Fостаток| / σшума
Rрепл = Nподтвержденных_серий / Nвсех_серий
ΔFмежлаб = |Fостаток_лаб1 - Fостаток_лаб2|
```

## 6. ПЕРЕМЕННЫЕ

| Переменная | Смысл |
| --- | --- |
| `P` | вес тела или показание весового канала |
| `m` | масса объекта |
| `g` | локальное ускорение свободного падения |
| `ΔP` | изменение показания веса между контрольным и активным режимом |
| `Fостаток` | сила после вычитания известных вкладов |
| `SNR` | отношение сигнала к шуму |
| `Rрепл` | доля подтвержденных повторений |
| `ΔFмежлаб` | расхождение между лабораториями |

## 7. ЕДИНИЦЫ ИЗМЕРЕНИЯ

| Величина | Единица |
| --- | --- |
| масса | кг |
| сила / вес | Н |
| ускорение | м/с² |
| температура | К или °C |
| магнитная индукция | Тл |
| электрическое поле | В/м |
| давление | Па |
| время | с |

## 8. ПРИМЕР РАСЧЕТА

Пусть исходная серия дала `ΔP = 0,006 Н`. Известные вклады: механика `0,001 Н`, электромагнетизм `0,002 Н`, тепло и воздух `0,0015 Н`, датчик `0,0005 Н`.

```text
Fостаток = 0,006 - 0,001 - 0,002 - 0,0015 - 0,0005 = 0,001 Н
```

Если шум весового канала `σшума = 0,0008 Н`, то:

```text
SNR = 0,001 / 0,0008 = 1,25
```

Такой результат недостаточен для сильного вывода и должен получить статус `ТРЕБУЕТ ПОВТОРЕНИЯ`, а не `АНОМАЛИЯ ПОДТВЕРЖДЕНА`.

## 9. ЧТО ПРОВЕРЯЕТ НЕЗАВИСИМАЯ ЛАБОРАТОРИЯ

- совпадает ли численный прогноз с исходным протоколом;
- воспроизводится ли знак и масштаб эффекта;
- исчезает ли сигнал в контрольном режиме;
- сохраняется ли сигнал при смене оператора;
- сохраняется ли сигнал при смене датчика;
- объясняется ли сигнал известными силами;
- есть ли полная трассировка данных от датчика до таблицы;
- не был ли критерий успеха изменен после опыта.

## 10. КАКИЕ ДАННЫЕ НУЖНО СОБРАТЬ

- raw data всех серий;
- журнал включения режимов;
- калибровки до и после серии;
- фото и схема стенда;
- карта кабелей, заземления и источников питания;
- данные температуры, влажности, давления, вибрации;
- измерения `E`, `B`, токов, напряжений;
- список исключенных точек с причиной;
- расчет погрешностей;
- подписи ответственных операторов.

## 11. КАКИЕ ВЫВОДЫ ДЕЛАТЬ НЕЛЬЗЯ

Нельзя писать, что антигравитация доказана, если результат повторился только внутри одной команды, на одном стенде, без независимого датчика, без raw data или без учета отрицательных серий.

Нельзя заменять репликацию ссылкой на секретность, авторитет автора, патентную заявку, видео, пресс-релиз или недоступную внутреннюю таблицу.

## 12. КАК ОТЛИЧАТЬ НАУЧНУЮ РЕПЛИКАЦИЮ ОТ ПСЕВДОНАУЧНОЙ

Научная репликация заранее задает критерий провала и публикует отрицательные результаты. Псевдонаучная репликация меняет условия после неудачи, скрывает контрольные серии и объявляет любое несовпадение признаком особой чувствительности эффекта.

## 13. СЛЕДУЮЩИЕ ДОКУМЕНТЫ

Нужно подготовить протокол межлабораторного обмена данными, пакет слепого контроля, стандарт отчета об отрицательном результате и таблицу статусов репликации.
